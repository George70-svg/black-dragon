import styled from 'styled-components'

export const StyledNews = styled.div.attrs(() => ({
  className: 'news',
}))`
    grid-area: news;
    background-color: #8ca0ff;
    border: 1px solid black;
`
